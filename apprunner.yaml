version: 1.0
runtime: python3
build:
  commands:
    build:
      - yum install -y stockfish
      - git submodule update --init --recursive
      - pip install -r requirements.txt
run:
  runtime-version: 3.11
  command: python main.py
  network:
    port: 5000 